# Vaultwarden
Mount a Volume at `/data`, use HTTPS, set an admin token and disable open registration unless explicitly needed. Test backup and restore before storing any real credentials. Never commit secrets.
