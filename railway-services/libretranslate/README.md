# LibreTranslate
Set language/model options deliberately; models consume significant RAM and storage. Keep API keys and rate-limit configuration in Railway Variables. Validate supported languages after first deployment.
