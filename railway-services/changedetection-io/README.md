# changedetection.io
Mount a Volume at `/datastore`. Configure browser automation according to the upstream guide; a browser sidecar may need to be a separate Railway service. Add notification secrets only in Railway Variables.
