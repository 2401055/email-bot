# SearXNG
Railway candidate: Docker web service. Needs the upstream source/config, a persistent config volume if required, and search engine configuration. Public deployment must restrict abuse and rate-limit requests.
