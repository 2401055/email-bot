# VODER hosting note

The bridge files are included for reference, but the full VODER model stack is not marked Railway-ready. The current supported path is Kaggle/GPU. A Railway deployment would require a GPU-capable host or a redesign that calls an external inference service.
